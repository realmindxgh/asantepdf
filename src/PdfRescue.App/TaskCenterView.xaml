<UserControl x:Class="PdfRescue.App.TaskCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="24,22,24,14">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Task Center" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="Follow long-running PDF work without losing the rest of your workspace." Foreground="{StaticResource MutedTextBrush}" FontSize="14" Margin="0,6,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource FlatButtonStyle}" Click="ClearFinished_Click" Padding="13,7" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets"/><TextBlock Text="Clear finished" Margin="7,0,0,0"/></StackPanel>
            </Button>
        </Grid>

        <WrapPanel Grid.Row="1" Margin="24,0,24,16">
            <Border Style="{StaticResource PanelCardStyle}" Width="112" Margin="0,0,8,8" Padding="12"><StackPanel><TextBlock Text="Running" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/><TextBlock x:Name="RunningCount" Text="0" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel></Border>
            <Border Style="{StaticResource PanelCardStyle}" Width="112" Margin="0,0,8,8" Padding="12"><StackPanel><TextBlock Text="Queued" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/><TextBlock x:Name="QueuedCount" Text="0" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel></Border>
            <Border Style="{StaticResource PanelCardStyle}" Width="112" Margin="0,0,8,8" Padding="12"><StackPanel><TextBlock Text="Completed" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/><TextBlock x:Name="CompletedCount" Text="0" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel></Border>
            <Border Style="{StaticResource PanelCardStyle}" Width="112" Margin="0,0,8,8" Padding="12"><StackPanel><TextBlock Text="Failed" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/><TextBlock x:Name="FailedCount" Text="0" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel></Border>
            <Border Style="{StaticResource PanelCardStyle}" Width="112" Margin="0,0,8,8" Padding="12"><StackPanel><TextBlock Text="Cancelled" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/><TextBlock x:Name="CancelledCount" Text="0" FontSize="22" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel></Border>
            <ComboBox x:Name="FilterCombo" Width="190" Height="36" Margin="0,8,0,8" VerticalAlignment="Center" SelectionChanged="FilterCombo_SelectionChanged">
                <ComboBoxItem Content="All tasks" /><ComboBoxItem Content="Running / queued" /><ComboBoxItem Content="Completed" /><ComboBoxItem Content="Failed" /><ComboBoxItem Content="Cancelled" />
            </ComboBox>
        </WrapPanel>
        <Grid Grid.Row="2" Margin="24,0,24,24">
            <ListBox x:Name="TaskList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,9" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardLiftBorderStyle}" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#163251" CornerRadius="9" Padding="8,3" Margin="0,0,9,0"><TextBlock Text="{Binding StateLabel}" Foreground="#7DB7FF" FontSize="12" FontWeight="SemiBold"/></Border>
                                        <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Stage}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap" />
                                    <ProgressBar Height="6" Minimum="0" Maximum="1" Value="{Binding Progress}" Margin="0,10,0,0" />
                                    <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                                        <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0}%}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                        <TextBlock Text="  •  " Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                        <TextBlock Text="{Binding ElapsedLabel}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                        <TextBlock Text="  •  " Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                        <TextBlock Text="{Binding Type}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF777D" FontSize="12" Margin="0,7,0,0" TextWrapping="Wrap" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
                                    <Button Style="{StaticResource FlatButtonStyle}" Content="Result Options" Click="OpenOutput_Click"
                                            IsEnabled="{Binding CanOpenOutput}" ToolTip="Open result actions" Padding="12,6" />
                                    <Button Style="{StaticResource FlatButtonStyle}" Content="Retry" Click="RetryTask_Click"
                                            IsEnabled="{Binding CanRetry}" Padding="12,6" />
                                    <Button Style="{StaticResource FlatButtonStyle}" Content="Cancel" Click="CancelTask_Click"
                                            IsEnabled="{Binding CanCancel}" Padding="12,6" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel x:Name="EmptyTasks" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="38" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" />
                <TextBlock Text="No tasks yet" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,0" />
                <TextBlock Text="OCR, conversion, compression and other long-running work will appear here." Foreground="{StaticResource MutedTextBrush}" FontSize="12" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,5,0,0" MaxWidth="430" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
